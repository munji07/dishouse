# DISHOUSE 홈페이지 개발 기획서

## 1. 프로젝트 개요

DISHOUSE Discord 서버를 기반으로 한 **2D 인터랙티브 커뮤니티 홈페이지**를 제작한다.

일반적인 서버 소개 홈페이지가 아니라, 사용자가 Discord 계정으로 로그인한 뒤 **2D로 구현된 DISHOUSE 집 안을 직접 돌아다니며 다른 사용자와 대화하는 형태**를 목표로 한다.

핵심 컨셉:

> **Discord 서버를 하나의 집으로 표현하고, 실제 Discord 채팅을 집 안의 캐릭터들이 말하는 방식으로 보여준다.**

홈페이지와 Discord는 실시간으로 연결되어야 한다.

---

# 2. 핵심 기능

## 2.1 Discord 로그인

Discord OAuth2를 사용한다.

로그인한 사용자의 Discord 정보를 활용한다.

필요한 정보 예시:

- Discord User ID
- Discord username
- Discord display name
- Discord avatar
- Discord 서버 가입 여부
- Discord 역할
- 온라인/오프라인 상태가 필요한 경우 해당 상태 정보

### 로그인 후

사용자는 자신의 Discord 계정과 연결된 캐릭터로 DISHOUSE에 입장한다.

로그인하지 않은 사용자는 집 내부를 자유롭게 이용할 수 없도록 하고, 메인 화면에서 Discord 로그인 버튼을 제공한다.

---

# 3. 2D 집

홈페이지의 메인 화면은 2D Top-down 방식의 집이다.

집에는 현재 다음 방이 존재한다.

- 거실
- 침실
- 주방
- 방 1
- 방 2
- 화장실

각 방은 실제로 이동 가능한 독립적인 공간처럼 보여야 한다.

## 방 이동

사용자는 캐릭터를 직접 움직여 방 사이를 이동한다.

가능하면 문을 통과하는 자연스러운 이동 연출을 사용한다.

단순히 버튼을 눌러 순간이동하는 방식은 기본 방식으로 사용하지 않는다.

---

# 4. Discord 채널 ↔ 방 연결

각 방은 Discord 채널 하나와 연결된다.

관리자는 Discord에서 슬래시 명령어를 이용해 연결한다.

기본 명령어:

```text
/채널지정 (방 이름) (채널)
```

예:

```text
/채널지정 거실 #일반
/채널지정 침실 #일상
/채널지정 주방 #요리
/채널지정 방1 #게임
/채널지정 방2 #공부
/채널지정 화장실 #잡담
```

## 중요

DB에는 Discord 채널 이름이 아니라 **Discord Channel ID**를 저장한다.

예:

```text
room: living
channelId: "123456789012345678"
```

Discord에서 채널 이름이 변경되어도 연결이 유지되어야 한다.

---

# 5. 방별 채팅

사용자가 방에 들어가면 해당 방에 연결된 Discord 채널이 현재 채팅 채널이 된다.

예:

```text
거실
↓
#일반
```

방 1로 이동:

```text
방 1
↓
#게임
```

사용자가 홈페이지에서 메시지를 입력하면 현재 방에 연결된 Discord 채널로 메시지를 전송한다.

반대로 Discord에서 해당 채널에 메시지가 올라오면 홈페이지의 해당 방에 있는 사용자에게 실시간으로 표시한다.

---

# 6. Discord ↔ 홈페이지 실시간 연동

핵심 기능이다.

### Discord → 홈페이지

Discord:

```text
먼지: 오늘 마크 할 사람?
```

홈페이지:

```text
        [먼지 캐릭터]

       ┌─────────────────┐
       │ 오늘 마크 할 사람? │
       └─────────────────┘
```

캐릭터 머리 위에 말풍선이 표시된다.

### 홈페이지 → Discord

홈페이지에서:

```text
오늘 마크 할 사람?
```

입력하면 연결된 Discord 채널에 실제 메시지가 전송되어야 한다.

---

# 7. 캐릭터

## 7.1 Discord 프로필 사진 기반 캐릭터

사용자의 Discord 프로필 사진을 **캐릭터의 머리/얼굴 부분**으로 사용한다.

프사 전체를 몸통으로 붙이는 방식은 사용하지 않는다.

구조:

```text
     ┌─────────┐
     │ Discord │
     │  Avatar │
     └────┬────┘
          │
        ┌─┼─┐
          │
         / \
```

### 캐릭터 구성

- 머리/얼굴: Discord Avatar
- 몸: 공통 2D 픽셀 캐릭터
- 닉네임: 캐릭터 위
- 말풍선: 캐릭터 위
- 상태 아이콘: 필요 시 추가

Discord 프로필 사진은 원형 또는 캐릭터에 자연스럽게 맞는 형태로 처리한다.

작은 화면에서도 프사가 선명하게 보이도록 적절히 리사이징/캐싱한다.

---

# 8. 캐릭터 표시

캐릭터 위에는 Discord display name을 표시한다.

예:

```text
       먼지
       🖼️
       🧍
```

다른 사용자도 동일한 방식으로 표시한다.

마우스를 캐릭터 위에 올리면 간단한 Discord 프로필 카드를 표시한다.

예:

```text
┌────────────────────┐
│     [프로필 사진]   │
│                    │
│ 먼지                │
│ 🟢 온라인           │
│                    │
│ 현재 위치: 거실     │
└────────────────────┘
```

불필요하게 큰 프로필 UI는 만들지 않는다.

---

# 9. 말풍선

Discord에서 메시지가 들어오거나 홈페이지에서 메시지를 보내면 해당 캐릭터 위에 말풍선을 표시한다.

예:

```text
        ┌──────────────┐
        │ 안녕하세요!  │
        └──────┬───────┘
               🖼️
               🧍
```

말풍선은 일정 시간 후 자연스럽게 사라진다.

너무 긴 메시지는 적절히 줄이거나 여러 줄로 표시한다.

채팅 기록은 별도의 채팅 UI에서 확인할 수 있어야 한다.

---

# 10. 현재 접속자

홈페이지에 현재 접속자 수를 작게 표시한다.

예:

```text
● 12명 온라인
```

클릭하면 방별 인원 현황을 보여줄 수 있다.

```text
🛋 거실       5명
🛏 침실       1명
🍳 주방       2명
🚪 방 1       4명
🚪 방 2       0명
🚿 화장실     0명
```

초기 구현에서는 간단한 온라인 인원 표시만 구현해도 된다.

---

# 11. UI 디자인

## 방향

**이쁘고 간결한 UI**를 최우선으로 한다.

일반적인 관리자 대시보드나 웹사이트처럼 보이면 안 된다.

게임과 웹사이트의 중간 느낌을 목표로 한다.

2D 집과 캐릭터가 화면의 중심이어야 하며 UI는 최대한 방해하지 않도록 한다.

### 기본 화면 예시

```text
┌─────────────────────────────────────────────┐
│ DISHOUSE                             👤 ⚙   │
│                                             │
│                                             │
│              🛋️  거실                        │
│                                             │
│       먼지 🖼️🧍       🖼️🧍 유저1            │
│             💬 안녕하세요                   │
│                                             │
│                         🚪 방 1              │
│                                             │
│                                             │
│ ┌─────────────────────────────────────────┐ │
│ │ 메시지를 입력하세요...                ➤ │ │
│ └─────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

UI가 게임 화면을 과도하게 가리지 않도록 한다.

---

# 12. 방이 연결되지 않은 경우

모든 방이 반드시 Discord 채널과 연결되어 있어야 하는 것은 아니다.

연결되지 않은 방은:

```text
이 방은 아직 연결된 채널이 없습니다.
```

등으로 표시한다.

채팅 입력창은 비활성화하거나 숨긴다.

---

# 13. 관리자 명령어

기본:

```text
/채널지정 (방 이름) (채널)
```

추가로 구현하면 좋은 명령어:

```text
/채널정보
```

결과:

```text
🏠 DISHOUSE 방 채널 설정

🛋 거실      → #일반
🛏 침실      → #일상
🍳 주방      → #요리
🚪 방 1      → #게임
🚪 방 2      → #공부
🚿 화장실    → #잡담
```

추후:

```text
/채널초기화 (방 이름)
```

도 고려한다.

관리 명령어는 관리자 또는 지정된 권한을 가진 사용자만 실행할 수 있어야 한다.

---

# 14. 권한

Discord 서버의 권한 체계를 최대한 활용한다.

예:

- 관리자
- 서버 관리 권한
- 특정 관리자 역할

일반 사용자는 방 채널 설정 명령어를 사용할 수 없어야 한다.

홈페이지에서도 Discord 서버 가입 여부와 필요한 권한을 서버에서 검증한다.

---

# 15. 기술 방향

권장 구조:

```text
Frontend
├─ Next.js
├─ 2D Renderer
└─ WebSocket Client

Backend
├─ Node.js / Next.js Server
├─ Discord OAuth2
├─ Discord Bot
└─ WebSocket Server

Database
└─ PostgreSQL / Supabase
```

2D 엔진은 Phaser 등을 사용할 수 있다.

단, 프로젝트 규모와 기존 구조에 따라 Canvas 기반 자체 구현도 검토한다.

---

# 16. 데이터 구조 예시

## rooms

```text
id
name
channel_id
position / map data
created_at
updated_at
```

예:

```json
{
  "id": "living",
  "name": "거실",
  "channel_id": "123456789012345678"
}
```

## users

```text
discord_id
display_name
avatar_url
character_data
last_position
last_seen
```

캐릭터 위치는 필요하다면 DB에 저장한다.

다만 실시간 위치 데이터는 DB에 매 프레임 저장하지 않는다.

---

# 17. 실시간 위치

사용자가 움직일 때 모든 위치 데이터를 DB에 계속 저장하지 않는다.

실시간 위치:

```text
Client
↓
WebSocket
↓
Server
↓
Other Clients
```

DB에는 필요할 경우 마지막 위치 정도만 저장한다.

---

# 18. 보안

절대로 클라이언트에 Discord Bot Token을 노출하지 않는다.

Discord API를 통한 메시지 전송은 서버에서 처리한다.

구조:

```text
Browser
  ↓
Backend
  ↓
Discord API
```

Bot Token은 서버 환경변수로만 관리한다.

OAuth2 역시 서버 측에서 안전하게 처리한다.

---

# 19. 채팅 처리

홈페이지 채팅:

```text
사용자 입력
↓
Backend
↓
현재 위치 확인
↓
해당 room의 channel_id 확인
↓
Discord 채널에 메시지 전송
↓
WebSocket broadcast
↓
홈페이지 캐릭터 말풍선
```

Discord 채팅:

```text
Discord Message
↓
Discord Bot
↓
메시지 channel_id 확인
↓
어떤 room에 연결되어 있는지 조회
↓
해당 room 사용자에게 WebSocket 전송
↓
캐릭터 말풍선 표시
```

---

# 20. 중요한 UX 원칙

1. 처음 방문한 사람도 별도의 설명 없이 사용할 수 있어야 한다.
2. 로그인은 Discord 하나로 최대한 간단하게 한다.
3. 2D 집이 화면의 주인공이어야 한다.
4. UI는 최소화한다.
5. 채팅은 Discord와 자연스럽게 연결되어야 한다.
6. 캐릭터의 움직임은 부드러워야 한다.
7. 말풍선은 짧고 자연스럽게 나타났다가 사라져야 한다.
8. 모바일에서도 최소한의 접근성을 확보한다.
9. 네트워크가 끊겨도 사용자에게 명확한 상태를 보여준다.
10. Discord 채널 이름 변경으로 방 연결이 깨지지 않도록 반드시 Channel ID를 기준으로 연결한다.

---

# 21. 초기 MVP 범위

처음부터 모든 기능을 구현하지 않는다.

### 반드시 구현

- Discord OAuth2 로그인
- Discord 서버 가입 확인
- 2D 집
- 6개 방
- 캐릭터 이동
- Discord Avatar를 캐릭터 머리로 사용
- 닉네임 표시
- 방 ↔ Discord Channel ID 연결
- `/채널지정`
- 홈페이지 → Discord 메시지 전송
- Discord → 홈페이지 메시지 수신
- WebSocket 실시간 동기화
- 캐릭터 말풍선
- 현재 접속자 표시
- 간결한 UI

### 나중에 구현

- 음성채널 연동
- 캐릭터 의상
- 펫
- 집 꾸미기
- 방 잠금
- 방 이름 변경
- 시즌별 테마
- 개인 상태 메시지
- 프로필 카드 고도화
- 애니메이션/이펙트 강화

---

# 22. 최종 목표

DISHOUSE 홈페이지를 단순한 Discord 서버 홈페이지가 아니라,

> **"Discord 서버에 실제로 들어와서 집 안을 돌아다니며 사람들과 대화하는 온라인 공간"**

으로 만든다.

사용자가 접속했을 때 가장 먼저 느껴야 하는 것은:

```text
"웹사이트에 들어왔다"
```

가 아니라

```text
"디스하우스에 들어왔다"
```

라는 느낌이어야 한다.

핵심 경험은 다음 네 가지다.

```text
        🏠 2D 집
           +
        🧑 Discord 유저
           +
        💬 실시간 채팅
           +
        🔗 Discord 채널
```

이 네 요소를 중심으로 전체 UX와 기술 구조를 설계한다.
